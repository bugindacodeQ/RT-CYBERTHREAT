services:
  - type: web
    name: cyber-threat-detector
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python src/data_generation.py && python src/train.py
    startCommand: uvicorn src.app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true